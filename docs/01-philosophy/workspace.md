# Workspace — 团队知识的容器

## 什么是团队

团队是一群拥有**相似知识体系、相同知识范围**的人，为了某类共同事务组建的共同体。他们一起构建、维护特定的知识领域。

## Workspace 概念

Workspace 是 MeshMind 中**最主要的承载单元**。每个团队拥有独立的 Workspace，团队在其内部构建和共享知识。

## 核心特征

### 1. 知识隔离

Workspace 之间互相隔离。一个 Workspace 内的知识不会被其他 Workspace 访问，保证团队知识的独立性和安全性。

### 2. 租户映射

在系统中，Workspace 直接映射为**租户（Tenant）**。每个租户拥有：
- 独立的知识图谱
- 独立的知识生命周期
- 独立的检索空间
- 独立的成员与权限

### 3. 知识域

每个 Workspace 对应一个或多个**知识域（Knowledge Domain）**，代表该团队关注的知识范围。团队内的所有知识活动都围绕这些域展开。

## 设计含义

- Workspace 是 MeshMind 的最高层级组织单元
- 所有功能（提取、图谱、生命周期、检索）都在 Workspace 的范围内运作
- 跨 Workspace 的知识共享是未来的可能性，但 v1 不做
